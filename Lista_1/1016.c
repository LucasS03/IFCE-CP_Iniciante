#include <stdio.h>

int main() {

    int dist, tempo;
    scanf("%d", &dist); // Leio o valor de dis

    tempo = dist * 2; // Faço o calculo e atribuo a tempo

    printf("%d minutos\n", tempo); // printa o resultado

    return 0;
}
